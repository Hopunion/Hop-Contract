# v0.8 database changes

No database migration is required.

v0.8 keeps the existing combined `hop_name` value as the unique quantity key, while the frontend displays and edits it as separate **Variety** and **Format** fields.

Examples: `Citra T90`, `Citra T45`, `Citra HyperBoost Oil`.
