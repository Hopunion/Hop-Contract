# v1.0 database note

No live database migration is required from v0.8 to v1.0.

The new scenario values are stored inside the existing `app_settings.value` JSON. Named snapshots use the existing `forecast_snapshots` table. Column resizing/filtering and the persistent browser session are frontend-only.

Do not rerun `schema.sql` against an already configured live project.
