# v1.1 database change

Run `v1.1-migration.sql` in Supabase SQL Editor before deploying v1.1. Recipe hop lines are linked to exact Hop Inventory UUIDs and cloud save/load is updated to preserve those links.
